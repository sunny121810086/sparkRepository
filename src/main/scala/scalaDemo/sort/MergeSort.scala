package scalaDemo.sort

object MergeSort {
  def main(args: Array[String]): Unit = {

  }

  //两个有序的数组，进行归并排序
  def mergeSort(oneArr: Array[Int], otherArr: Array[Int]): Unit = {
    val mergeArr: Array[Int] = new Array[Int](oneArr.length + otherArr.length)

    var index = 0

  }
}
